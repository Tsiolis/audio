<!doctype html>
{%- comment -%} Use the request object to get the shop locale's iso code - https://shopify.dev/api/liquid/objects/request#request-locale {%- endcomment -%}
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <link rel="canonical" href="{{ canonical_url }}">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Jost:wght@400;700&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="https://unpkg.com/flickity@2/dist/flickity.min.css">
	<link rel="icon" type="image/png" href=" {{ settings.favicon | | img_url: '32x32' }} ">


  {%- comment -%} Exercise: Add the social share image that is set in the admin - https://shopify.dev/api/liquid/objects/page-image{%- endcomment -%}

  {%- comment -%} page_title is taken from the search engine settings on your product, page, template, etc - https://shopify.dev/api/liquid/objects/page-title {%- endcomment -%}
  <title>{{ page_title }}</title>
  {%- comment -%} page_description is also taken from search engine settings and is escaped to prevent {%- endcomment -%}
  <meta name="description" content="{{ page_description | escape }}">

  {%- comment -%} Custom CSS loaded from the /assets folder {%- endcomment -%}
  {{ 'styles.css' | asset_url | stylesheet_tag }}

  {%- comment -%} jQuery is discouraged from being used in the theme store, but many custom themes still use it (but it is falling out of favour for performance reasons) https://shopify.dev/themes/best-practices/performance {%- endcomment -%}
  <script src="https://code.jquery.com/jquery-3.4.1.js" integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU=" crossorigin="anonymous" defer></script>

  {%- comment -%} Custom javascript loaded from the /assets folder {%- endcomment -%}
  <script src="{{ 'scripts.js' | asset_url }}" defer></script>

	
  {%- comment -%} content_for_header loads in Shopify scripts as well as any injected scripts from apps {%- endcomment -%}
  {{ content_for_header }}
</head>
<body>

	{%section 'announcement-bar' %}

  {%- comment -%} Static section that appears on all pages  {%- endcomment -%}
  {% section 'header' %}

  <main role="main">
    {%- comment -%} All drag & drop sections will be rendered here (content will change based on the page) {%- endcomment -%}
    {{ content_for_layout }}
  </main>

  {%- comment -%} Static section that appears on all pages  {%- endcomment -%}
  {% section 'footer' %}
	<script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>
</body>
</html>
